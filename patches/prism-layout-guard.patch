diff --git a/web/components/skins/prism/Prism.module.css b/web/components/skins/prism/Prism.module.css
index d7eec68..b6609a7 100644
--- a/web/components/skins/prism/Prism.module.css
+++ b/web/components/skins/prism/Prism.module.css
@@ -61,6 +61,29 @@
   isolation: isolate;
 }
 
+.stageGrid {
+  --prism-ui-scale: 1;
+  --prism-cover-size: clamp(16rem, 40vh, 30rem);
+  --prism-panel-gap: clamp(0.65rem, 1.45vh, 1rem);
+  --prism-footer-height: clamp(14rem, 29vh, 20rem);
+  min-height: 0;
+}
+
+.coverColumn {
+  min-height: 0;
+}
+
+.coverArt {
+  width: min(100%, var(--prism-cover-size));
+  max-width: 100%;
+  aspect-ratio: 1 / 1;
+  min-height: 0;
+}
+
+.contentStack {
+  min-height: 0;
+}
+
 .sideRow {
   position: relative;
   z-index: 0;
@@ -115,6 +138,30 @@
   opacity: 0.94;
 }
 
+.artistLine {
+  font-size: calc(1.45rem * var(--prism-ui-scale));
+  line-height: 1.15;
+}
+
+.albumLine {
+  font-size: calc(0.98rem * var(--prism-ui-scale));
+  line-height: 1.2;
+}
+
+.tagRail {
+  gap: calc(0.5rem * var(--prism-ui-scale));
+}
+
+.tagRail span {
+  padding: calc(0.25rem * var(--prism-ui-scale)) calc(0.5rem * var(--prism-ui-scale));
+  font-size: calc(0.68rem * var(--prism-ui-scale));
+  line-height: 1;
+}
+
+.boothCardWrap {
+  min-height: calc(4.5rem * var(--prism-ui-scale));
+}
+
 .signalFloor {
   display: none;
 }
@@ -148,8 +195,10 @@
 .controlButton {
   display: inline-grid;
   flex: 0 0 auto;
-  width: 2.75rem;
-  height: 2.75rem;
+  width: calc(2.75rem * var(--prism-ui-scale));
+  height: calc(2.75rem * var(--prism-ui-scale));
+  min-width: 2.08rem;
+  min-height: 2.08rem;
   place-items: center;
   border: 1px solid color-mix(in oklab, var(--line) 44%, transparent);
   background: color-mix(in oklab, var(--surface) 28%, transparent);
@@ -412,37 +461,85 @@
 }
 
 @media (min-width: 1180px) {
+  .stageGrid {
+    --prism-cover-size: clamp(15rem, 40vh, 30rem);
+    --prism-footer-height: clamp(13.5rem, 29vh, 19rem);
+    grid-template-columns: minmax(15rem, var(--prism-cover-size)) minmax(0, 1fr) !important;
+    grid-template-rows: minmax(0, calc(100% - var(--prism-footer-height))) var(--prism-footer-height);
+    align-items: center;
+    gap: clamp(1rem, 2vh, 1.6rem) clamp(1rem, 2.2vw, 1.6rem) !important;
+    padding: clamp(0.9rem, 2vh, 1.5rem) !important;
+    overflow: hidden;
+  }
+
+  .coverColumn {
+    align-self: center;
+    justify-content: center;
+  }
+
+  .coverArt {
+    height: auto;
+    max-height: 100%;
+    align-self: center;
+  }
+
+  .contentStack {
+    align-self: center;
+    gap: var(--prism-panel-gap);
+    overflow: visible;
+  }
+
+  .contentStack > div:first-child,
+  .contentStack > div:nth-child(2) {
+    min-height: 0;
+    overflow: hidden;
+  }
+
+  .title {
+    font-size: calc(clamp(2rem, 3.05vw, 3.05rem) * var(--prism-title-fit, 1));
+    max-height: 2.18em;
+  }
+
+  .micText {
+    max-height: clamp(2.4em, 6.8vh, 4.2em);
+    font-size: 0.92rem;
+  }
+
+  .transportProgress {
+    flex-shrink: 0;
+  }
+
   .signalMeter {
     margin-top: 0.2rem;
     margin-bottom: 0;
   }
 
   .controlRail {
-    margin-top: clamp(1.4rem, 3.2vh, 2.4rem);
+    flex-shrink: 0;
+    gap: calc(0.55rem * var(--prism-ui-scale));
+    margin-top: clamp(0.35rem, 1.2vh, 1rem);
   }
 
   .signalFloor {
-    position: absolute;
-    right: 1.5rem;
-    bottom: 1.35rem;
-    left: 1.5rem;
+    position: relative;
     z-index: 1;
+    grid-row: 2;
+    grid-column: 1 / -1;
     display: grid;
-    grid-template-columns: minmax(11rem, 1.25fr) minmax(8rem, 0.85fr) minmax(12rem, 1.4fr) minmax(10rem, 1.05fr);
-    gap: clamp(1.2rem, 3vw, 3rem);
-    padding-top: 1.05rem;
-    border-top: 1px solid color-mix(in oklab, var(--line) 26%, transparent);
+    grid-template-rows: clamp(1.9rem, 4vh, 2.7rem) minmax(0, 1fr);
+    gap: clamp(0.55rem, 1.05vh, 0.85rem);
+    height: var(--prism-footer-height);
+    min-height: 0;
+    padding-top: 0;
+    border-top: 0;
     color: color-mix(in oklab, var(--accent-2) 62%, transparent);
-    opacity: 0.62;
+    overflow: hidden;
   }
 
   .footerWave {
-    position: absolute;
-    top: -3.75rem;
-    right: 0;
-    left: 0;
+    position: relative;
     display: block;
-    height: 3.7rem;
+    min-height: 0;
     pointer-events: none;
     opacity: 0.22;
     mask-image: linear-gradient(90deg, transparent 0%, black 15%, black 85%, transparent 100%);
@@ -458,14 +555,31 @@
     background: color-mix(in oklab, var(--accent) 22%, transparent);
   }
 
-  .signalFloor div {
+  .footerPanel {
+    display: grid;
+    min-height: 0;
+    grid-template-columns: minmax(11rem, 0.85fr) minmax(22rem, 1.8fr) minmax(11rem, 0.85fr);
+    grid-template-rows: 1fr;
+    gap: clamp(0.95rem, 2vw, 1.8rem);
+    align-items: stretch;
+    padding: clamp(0.95rem, 1.8vh, 1.35rem) clamp(1rem, 2vw, 1.55rem);
+    border: 1px solid color-mix(in oklab, var(--line) 28%, transparent);
+    background: color-mix(in oklab, var(--bg) 38%, transparent);
+    box-shadow: inset 0 1px 0 color-mix(in oklab, var(--ink) 6%, transparent);
+    backdrop-filter: blur(18px);
+    overflow: hidden;
+  }
+
+  .footerMeta,
+  .footerLyrics {
     min-width: 0;
+    overflow: hidden;
   }
 
   .signalFloor p {
     margin-top: 0.38rem;
     overflow: hidden;
-    font-size: 0.78rem;
+    font-size: calc(0.84rem * var(--prism-ui-scale));
     font-weight: 720;
     line-height: 1.2;
     text-overflow: ellipsis;
@@ -473,28 +587,127 @@
     white-space: nowrap;
   }
 
-  .weatherReport {
-    display: grid;
+  .footerLyrics {
+    display: flex;
+    min-height: 0;
+    flex-direction: column;
+    text-align: left;
+  }
+
+  .lyricsHeader {
+    display: flex;
+    min-height: 1.65rem;
+    flex-shrink: 0;
+    align-items: flex-start;
+    justify-content: space-between;
+    gap: 0.75rem;
+  }
+
+  .lyricsTools {
+    display: flex;
     min-width: 0;
-    gap: 0.18rem;
+    align-items: center;
+    gap: 0.45rem;
+    font-family: var(--font-mono);
+    font-size: 0.58rem;
+    font-weight: 900;
+    letter-spacing: 0.14em;
+    text-transform: uppercase;
+    color: color-mix(in oklab, var(--accent-2) 62%, transparent);
   }
 
-  .weatherReport p {
-    margin-top: 0.38rem;
+  .lyricsToolButton {
+    display: inline-flex;
+    height: 1.55rem;
+    align-items: center;
+    gap: 0.32rem;
+    border: 1px solid color-mix(in oklab, var(--line) 42%, transparent);
+    background: color-mix(in oklab, var(--surface) 22%, transparent);
+    padding: 0 0.45rem;
+    color: var(--accent);
+    font: inherit;
+    letter-spacing: 0.08em;
+    cursor: pointer;
+  }
+
+  .lyricsOffset {
+    display: flex;
+    min-height: 1.75rem;
+    flex-shrink: 0;
+    align-items: center;
+    gap: 0.45rem;
+    border-top: 1px solid color-mix(in oklab, var(--line) 20%, transparent);
+    padding-top: 0.35rem;
+  }
+
+  .lyricsOffset button {
+    height: 1.45rem;
+    flex-shrink: 0;
+    border: 1px solid color-mix(in oklab, var(--line) 38%, transparent);
+    background: color-mix(in oklab, var(--surface) 20%, transparent);
+    padding: 0 0.45rem;
+    color: var(--accent);
+    font-family: var(--font-mono);
+    font-size: 0.58rem;
+    font-weight: 900;
+    letter-spacing: 0.08em;
+    text-transform: uppercase;
+    cursor: pointer;
+  }
+
+  .lyricsOffset input {
+    min-width: 0;
+    flex: 1;
+    accent-color: var(--accent);
+  }
+
+  .lyricsScroller {
+    min-height: 0;
+    flex: 1;
+    overflow-y: auto;
+    padding: 0.35rem 0.35rem 0.2rem 0;
+    scrollbar-width: thin;
+    scrollbar-color: color-mix(in oklab, var(--accent) 62%, transparent) transparent;
+  }
+
+  .lyricsScroller p,
+  .lyricLine {
+    margin-top: 0;
+    overflow: visible;
+    font-size: 0.95rem;
+    font-weight: 720;
+    line-height: 1.35;
+    text-transform: none;
+    white-space: normal;
     color: color-mix(in oklab, var(--ink) 78%, var(--accent) 22%);
   }
 
-  .weatherReport span {
-    color: color-mix(in oklab, var(--accent-2) 54%, transparent);
+  .lyricLine {
+    border-left: 1px solid color-mix(in oklab, var(--line) 34%, transparent);
+    padding: 0.34rem 0.6rem;
+    transition: background-color 160ms ease, border-color 160ms ease, color 160ms ease;
   }
 
-  .footerDetail,
-  .weatherReport span {
+  .activeLyricLine {
+    border-color: var(--accent);
+    background: color-mix(in oklab, var(--accent) 10%, transparent);
+    color: var(--ink);
+  }
+
+  .plainLyricLine {
+    color: color-mix(in oklab, var(--ink) 82%, var(--accent) 18%);
+  }
+
+  .footerMeta:last-child {
+    text-align: right;
+  }
+
+  .footerDetail {
     display: block;
-    margin-top: 0.18rem;
+    margin-top: 0.16rem;
     min-width: 0;
     overflow: hidden;
-    font-size: 0.68rem;
+    font-size: calc(0.72rem * var(--prism-ui-scale));
     font-weight: 720;
     letter-spacing: 0.1em;
     line-height: 1.1;
@@ -503,6 +716,120 @@
     white-space: nowrap;
     color: color-mix(in oklab, var(--accent-2) 54%, transparent);
   }
+
+  .sideRail {
+    height: 100%;
+    max-height: 100%;
+    grid-template-rows: minmax(4.9rem, auto) minmax(0, 1fr) minmax(5.4rem, 0.72fr) !important;
+    overflow: hidden;
+  }
+
+  .sideRail > section {
+    min-height: 0 !important;
+    height: auto !important;
+  }
+}
+
+@media (min-width: 1180px) and (max-height: 820px) {
+  .stageGrid {
+    --prism-cover-size: clamp(12rem, 35vh, 22rem);
+    --prism-panel-gap: 0.62rem;
+    --prism-footer-height: clamp(11.5rem, 28vh, 15rem);
+    grid-template-rows: minmax(0, calc(100% - var(--prism-footer-height))) var(--prism-footer-height);
+  }
+
+  .title {
+    font-size: calc(clamp(1.85rem, 2.7vw, 2.6rem) * var(--prism-title-fit, 1));
+    max-height: 2.15em;
+  }
+
+  .micText {
+    max-height: 2.9em;
+  }
+
+  .tagRail {
+    margin-top: 0.85rem;
+  }
+
+  .signalFloor {
+    grid-template-rows: clamp(1.4rem, 3.3vh, 2rem) minmax(0, 1fr);
+  }
+
+  .footerPanel {
+    padding: 0.75rem 0.9rem;
+  }
+
+  .signalFloor p {
+    margin-top: 0.2rem;
+    font-size: 0.76rem;
+  }
+}
+
+@media (min-width: 1180px) and (max-height: 720px) {
+  .stageGrid {
+    --prism-cover-size: clamp(9.5rem, 29vh, 16rem);
+    --prism-panel-gap: 0.48rem;
+    --prism-footer-height: clamp(9rem, 27vh, 11.5rem);
+    grid-template-rows: minmax(0, calc(100% - var(--prism-footer-height))) var(--prism-footer-height);
+  }
+
+  .contentStack {
+    gap: var(--prism-panel-gap);
+  }
+
+  .title {
+    font-size: calc(clamp(1.65rem, 2.35vw, 2.18rem) * var(--prism-title-fit, 1));
+    max-height: 2.12em;
+  }
+
+  .micText {
+    max-height: 2.55em;
+  }
+
+  .controlRail {
+    margin-top: 0.15rem;
+  }
+
+  .signalFloor {
+    display: grid;
+    grid-template-rows: clamp(0.9rem, 2.6vh, 1.5rem) minmax(0, 1fr);
+  }
+
+  .footerPanel {
+    grid-template-columns: minmax(7rem, 0.9fr) minmax(12rem, 1.45fr) minmax(7rem, 0.9fr);
+    gap: 0.45rem;
+    padding: 0.5rem 0.65rem;
+  }
+
+  .signalFloor p,
+  .footerDetail {
+    margin-top: 0.1rem;
+    font-size: 0.68rem;
+    line-height: 1.12;
+  }
+
+  .sideRail {
+    gap: 0.45rem;
+    grid-template-rows: minmax(3.9rem, auto) minmax(0, 1fr) minmax(4.2rem, 0.65fr) !important;
+  }
+
+  .sideRow {
+    padding-top: 0.22rem;
+    padding-bottom: 0.22rem;
+  }
+
+  .sideTrackTitle {
+    font-size: 0.68rem;
+    line-height: 1.12;
+  }
+
+  .sideMeta {
+    font-size: 0.58rem;
+  }
+
+  .sideAccent {
+    font-size: 0.54rem;
+  }
 }
 
 .progress {
diff --git a/web/components/skins/prism/PrismSkin.tsx b/web/components/skins/prism/PrismSkin.tsx
index a2e81ad..b6b047b 100644
--- a/web/components/skins/prism/PrismSkin.tsx
+++ b/web/components/skins/prism/PrismSkin.tsx
@@ -5,7 +5,7 @@
 
 import { useEffect, useMemo, useRef, useState, type RefObject } from 'react';
 import * as Dialog from '@radix-ui/react-dialog';
-import { Heart, Info, LoaderCircle, Play, RadioTower, Send, Square, Volume2, VolumeX, X } from 'lucide-react';
+import { Heart, Info, LoaderCircle, Play, RadioTower, Send, SlidersHorizontal, Square, Volume2, VolumeX, X } from 'lucide-react';
 import styles from './Prism.module.css';
 import {
   usePlayerActions,
@@ -45,6 +45,41 @@ function Label({ children }: { children: React.ReactNode }) {
   );
 }
 
+const LYRICS_OFFSET_KEY = 'subwave:lyrics-offset-ms';
+
+interface LyricLine {
+  text: string;
+  startMs?: number | null;
+}
+
+interface CurrentLyricsPayload {
+  songId?: string | null;
+  synced?: boolean;
+  lines: LyricLine[];
+}
+
+function clampLyricOffset(value: number): number {
+  return Number.isFinite(value) ? Math.min(5000, Math.max(-5000, Math.round(value))) : 0;
+}
+
+function formatLyricOffset(value: number): string {
+  if (value === 0) return '0.00s';
+  return `${value > 0 ? '+' : '-'}${(Math.abs(value) / 1000).toFixed(2)}s`;
+}
+
+function activeLyricIndex(payload: CurrentLyricsPayload | null, elapsedMs: number, offsetMs: number): number {
+  if (!payload?.synced) return -1;
+  const target = Math.max(0, elapsedMs + offsetMs);
+  let active = -1;
+  for (let i = 0; i < payload.lines.length; i += 1) {
+    const startMs = payload.lines[i]?.startMs;
+    if (startMs == null) continue;
+    if (startMs > target) break;
+    active = i;
+  }
+  return active;
+}
+
 function Tag({ children }: { children: React.ReactNode }) {
   return (
     <span className="border border-[var(--line)] bg-[color-mix(in_oklab,var(--bg)_34%,transparent)] px-2 py-1 font-mono text-[11px] font-bold text-[var(--accent-2)] uppercase">
@@ -338,6 +373,13 @@ export default function PrismSkin(_props: SkinProps) {
   const [playerUrl, setPlayerUrl] = useState('');
   const titleRef = useRef<HTMLHeadingElement | null>(null);
   const [titleFit, setTitleFit] = useState(1);
+  const [lyricsPayload, setLyricsPayload] = useState<CurrentLyricsPayload | null>(null);
+  const [lyricsLoading, setLyricsLoading] = useState(false);
+  const [lyricsFailed, setLyricsFailed] = useState(false);
+  const [lyricsNow, setLyricsNow] = useState(() => Date.now());
+  const [lyricsOffsetMs, setLyricsOffsetMs] = useState(0);
+  const [lyricsOffsetOpen, setLyricsOffsetOpen] = useState(false);
+  const activeLyricRef = useRef<HTMLDivElement | null>(null);
   const like = useTrackLike();
   const adjustVolume = useVolumeNudge();
   const volumePercent = Math.round(volume * 100);
@@ -372,6 +414,62 @@ export default function PrismSkin(_props: SkinProps) {
     return () => window.clearInterval(id);
   }, []);
 
+  useEffect(() => {
+    try {
+      const stored = window.localStorage.getItem(LYRICS_OFFSET_KEY);
+      if (stored != null) setLyricsOffsetMs(clampLyricOffset(Number(stored)));
+    } catch {}
+  }, []);
+
+  const setLyricOffset = (next: number) => {
+    const clamped = clampLyricOffset(next);
+    setLyricsOffsetMs(clamped);
+    try {
+      window.localStorage.setItem(LYRICS_OFFSET_KEY, String(clamped));
+    } catch {}
+  };
+
+  useEffect(() => {
+    let cancelled = false;
+    setLyricsPayload(null);
+    setLyricsFailed(false);
+    if (!coverId || offline) {
+      setLyricsLoading(false);
+      return;
+    }
+    setLyricsLoading(true);
+    fetch(`${client.origin.apiUrl}/lyrics/current`, { cache: 'no-store' })
+      .then(r => (r.ok ? r.json() as Promise<CurrentLyricsPayload> : null))
+      .then(data => {
+        if (cancelled) return;
+        setLyricsPayload(data?.songId === coverId ? data : null);
+        setLyricsFailed(data == null);
+      })
+      .finally(() => {
+        if (!cancelled) setLyricsLoading(false);
+      });
+    return () => {
+      cancelled = true;
+    };
+  }, [client, coverId, offline]);
+
+  useEffect(() => {
+    if (!lyricsPayload?.synced) return;
+    const id = window.setInterval(() => setLyricsNow(Date.now()), 500);
+    return () => window.clearInterval(id);
+  }, [lyricsPayload?.synced, trackStartedAt]);
+
+  const lyricElapsedMs = trackStartedAt == null ? 0 : Math.max(0, lyricsNow - trackStartedAt);
+  const activeLyric = useMemo(
+    () => activeLyricIndex(lyricsPayload, lyricElapsedMs, lyricsOffsetMs),
+    [lyricsPayload, lyricElapsedMs, lyricsOffsetMs],
+  );
+
+  useEffect(() => {
+    if (activeLyric < 0) return;
+    activeLyricRef.current?.scrollIntoView({ block: 'center', behavior: 'smooth' });
+  }, [activeLyric]);
+
   useEffect(() => {
     let cancelled = false;
     (async () => {
@@ -433,24 +531,24 @@ export default function PrismSkin(_props: SkinProps) {
             <img
               src={coverSrc}
               alt=""
-              className="absolute inset-[-7%] h-[114%] w-[114%] scale-105 object-cover opacity-40 blur-3xl saturate-150"
+              className="absolute inset-[-7%] h-[114%] w-[114%] scale-105 object-cover opacity-55 blur-3xl saturate-150"
             />
             <img
               src={coverSrc}
               alt=""
-              className="absolute inset-0 h-full w-full object-cover opacity-8 blur-[1px] saturate-125"
+              className="absolute inset-0 h-full w-full object-cover opacity-[0.14] blur-[1px] saturate-125"
             />
           </>
         )}
       </div>
 
       <div className="relative z-10 flex h-full min-h-0 w-full max-w-full flex-col overflow-hidden px-4 py-4 sm:px-6 sm:py-5">
-        <div className="grid min-h-0 min-w-0 flex-1 grid-cols-1 gap-4 overflow-y-auto min-[1180px]:grid-cols-[minmax(0,1fr)_minmax(270px,320px)] min-[1180px]:overflow-hidden">
-          <main className={cn('relative overflow-visible border min-[1180px]:min-h-0 min-[1180px]:overflow-hidden', styles.glass)}>
+        <div className="grid h-full min-h-0 min-w-0 flex-1 grid-cols-1 gap-4 overflow-y-auto min-[1180px]:grid-cols-[minmax(0,1fr)_minmax(270px,320px)] min-[1180px]:overflow-hidden">
+          <main className={cn('relative h-full overflow-visible border min-[1180px]:min-h-0 min-[1180px]:overflow-hidden', styles.glass)}>
             <div className={cn('pointer-events-none absolute inset-0', styles.stageScrim)} aria-hidden="true" />
-            <div className="relative z-10 grid min-w-0 grid-cols-1 gap-6 p-4 min-[1180px]:h-full min-[1180px]:min-h-0 min-[1180px]:grid-cols-[minmax(300px,43vh)_minmax(0,1fr)] min-[1180px]:p-6 xl:grid-cols-[minmax(360px,52vh)_minmax(0,1fr)]">
-              <div className="contents min-[1180px]:flex min-[1180px]:min-w-0 min-[1180px]:flex-col min-[1180px]:justify-start min-[1180px]:gap-5">
-                <div className="relative order-1 aspect-square w-full max-w-[460px] self-center overflow-hidden border border-[color-mix(in_oklab,var(--line)_62%,transparent)] bg-[color-mix(in_oklab,var(--bg)_34%,transparent)] shadow-2xl shadow-[color-mix(in_oklab,var(--bg)_72%,transparent)] min-[1180px]:max-w-none min-[1180px]:self-auto">
+            <div className={cn('relative z-10 grid min-w-0 grid-cols-1 gap-6 p-4 min-[1180px]:h-full min-[1180px]:min-h-0 min-[1180px]:grid-cols-[minmax(300px,43vh)_minmax(0,1fr)] min-[1180px]:p-6 xl:grid-cols-[minmax(360px,52vh)_minmax(0,1fr)]', styles.stageGrid)}>
+              <div className={cn('contents min-[1180px]:flex min-[1180px]:min-w-0 min-[1180px]:flex-col min-[1180px]:justify-start min-[1180px]:gap-5', styles.coverColumn)}>
+                <div className={cn('relative order-1 aspect-square w-full max-w-[460px] self-center overflow-hidden border border-[color-mix(in_oklab,var(--line)_62%,transparent)] bg-[color-mix(in_oklab,var(--bg)_34%,transparent)] shadow-2xl shadow-[color-mix(in_oklab,var(--bg)_72%,transparent)] min-[1180px]:max-w-none min-[1180px]:self-auto', styles.coverArt)}>
                   {coverSrc && !offline ? (
                     <img src={coverSrc} alt={nowPlaying?.album || nowPlaying?.title || ''} className="h-full w-full object-cover" />
                   ) : (
@@ -463,7 +561,7 @@ export default function PrismSkin(_props: SkinProps) {
                 <div className="order-3 min-[1180px]:hidden" />
               </div>
 
-              <div className="order-2 flex min-w-0 flex-col justify-start gap-4 py-2 min-[1180px]:order-none min-[1180px]:h-full min-[1180px]:py-0">
+              <div className={cn('order-2 flex min-w-0 flex-col justify-start gap-4 py-2 min-[1180px]:order-none min-[1180px]:h-full min-[1180px]:py-0', styles.contentStack)}>
                 <div className="min-w-0">
                   <span className="bg-[var(--accent)] px-2 py-1 font-mono text-[10px] font-black text-bg uppercase">
                     {nowPlayingLabel}
@@ -481,24 +579,24 @@ export default function PrismSkin(_props: SkinProps) {
                     )}
                   </h1>
                   {artist && (
-                    <p className="mt-3 text-xl font-semibold text-[var(--accent)] xl:text-2xl">
+                    <p className={cn('mt-3 font-semibold text-[var(--accent)]', styles.artistLine)}>
                       {artist}
                     </p>
                   )}
                   {(nowPlaying?.album || nowPlaying?.year) && !offline && (
-                    <p className="mt-1 text-sm text-[var(--accent-2)] xl:text-base">
+                    <p className={cn('mt-1 text-[var(--accent-2)]', styles.albumLine)}>
                       {nowPlaying?.album || 'Unknown album'}
                       {nowPlaying?.year ? ` / ${nowPlaying.year}` : ''}
                     </p>
                   )}
                   {heroTags.length > 0 && (
-                    <div className="mt-5 flex flex-wrap items-center gap-2">
+                    <div className={cn('mt-5 flex flex-wrap items-center gap-2', styles.tagRail)}>
                       {heroTags.map(tag => <Tag key={tag}>{tag}</Tag>)}
                     </div>
                   )}
                 </div>
 
-                <div className="min-h-[72px]">
+                <div className={styles.boothCardWrap}>
                   <div className="border-l-2 border-[var(--accent)] bg-[color-mix(in_oklab,var(--bg)_30%,transparent)] px-3 py-2.5 backdrop-blur-md">
                     <div className="mb-1 flex items-center justify-between gap-2">
                       <Label>From the booth</Label>
@@ -658,39 +756,98 @@ export default function PrismSkin(_props: SkinProps) {
                 </div>
               </div>
 
-              <div className={styles.signalFloor} aria-hidden="true">
+              <div className={styles.signalFloor}>
                 <div className={styles.footerWave}>
                   <PrismWaveform audioRef={audioRef} active={playing} />
                 </div>
-                <div>
-                  <Label>Weather</Label>
-                  {weatherLine ? (
-                    <p>{weatherLine}</p>
-                  ) : (
-                    <div className={styles.weatherReport}>
-                      <p>{weatherSummary || 'No report'}</p>
-                      {weatherLocation && <span>{weatherLocation}</span>}
+                <div className={styles.footerPanel}>
+                  <div className={styles.footerMeta}>
+                    <Label>Station</Label>
+                    <p>{weatherLine || stationName}</p>
+                    <span className={styles.footerDetail}>
+                      {weatherSummary || weatherLocation || statusLine}
+                    </span>
+                  </div>
+                  <div className={styles.footerLyrics}>
+                    <div className={styles.lyricsHeader}>
+                      <Label>Lyrics</Label>
+                      <div className={styles.lyricsTools}>
+                        {lyricsPayload?.synced && (
+                          <button
+                            type="button"
+                            onClick={() => setLyricsOffsetOpen(value => !value)}
+                            className={cn(styles.lyricsToolButton, lyricsOffsetOpen && styles.activeControl)}
+                            aria-pressed={lyricsOffsetOpen}
+                            aria-label="Configure lyric timing offset"
+                            title="Configure lyric timing offset"
+                          >
+                            <SlidersHorizontal aria-hidden className="size-3.5" />
+                            <span>{formatLyricOffset(lyricsOffsetMs)}</span>
+                          </button>
+                        )}
+                        <span>{lyricsPayload?.synced ? 'Synced' : 'Plain'}</span>
+                      </div>
                     </div>
-                  )}
-                </div>
-                <div>
-                  <Label>Station time</Label>
-                  <p suppressHydrationWarning>{stationTime}</p>
-                </div>
-                <div>
-                  <Label>Program</Label>
-                  <p>{showLine}</p>
-                  {showTiming?.until && <span className={styles.footerDetail}>{showTiming.until}</span>}
-                </div>
-                <div>
-                  <Label>Up next</Label>
-                  <p>{showTiming?.next || 'Checking schedule'}</p>
+
+                    {lyricsPayload?.synced && lyricsOffsetOpen && (
+                      <div className={styles.lyricsOffset}>
+                        <button type="button" onClick={() => setLyricOffset(lyricsOffsetMs - 100)} aria-label="Delay lyrics">-</button>
+                        <input
+                          type="range"
+                          min={-5000}
+                          max={5000}
+                          step={50}
+                          value={lyricsOffsetMs}
+                          onChange={event => setLyricOffset(Number(event.target.value))}
+                          aria-label="Lyric offset"
+                        />
+                        <button type="button" onClick={() => setLyricOffset(lyricsOffsetMs + 100)} aria-label="Advance lyrics">+</button>
+                        <button type="button" onClick={() => setLyricOffset(0)}>Zero</button>
+                      </div>
+                    )}
+
+                    <div className={styles.lyricsScroller}>
+                      {!coverId || offline ? (
+                        <p>Lyrics are available for library tracks once the station has indexed them.</p>
+                      ) : lyricsLoading ? (
+                        <p>Pulling lyrics from the library...</p>
+                      ) : lyricsFailed ? (
+                        <p>Lyrics are not reachable right now.</p>
+                      ) : !lyricsPayload?.lines.length ? (
+                        <p>{nowPlaying?.title ? `${nowPlaying.title} has no lyrics indexed yet.` : 'No lyrics indexed yet.'}</p>
+                      ) : (
+                        lyricsPayload.lines.map((line, index) => {
+                          const active = index === activeLyric;
+                          return (
+                            <div
+                              key={`${line.startMs ?? 'plain'}-${index}`}
+                              ref={active ? activeLyricRef : undefined}
+                              className={cn(
+                                styles.lyricLine,
+                                active && styles.activeLyricLine,
+                                !lyricsPayload.synced && styles.plainLyricLine,
+                              )}
+                            >
+                              {line.text}
+                            </div>
+                          );
+                        })
+                      )}
+                    </div>
+                  </div>
+                  <div className={styles.footerMeta}>
+                    <Label>Program</Label>
+                    <p>{showLine}</p>
+                    <span className={styles.footerDetail}>
+                      {showTiming?.until || stationTime}
+                    </span>
+                  </div>
                 </div>
               </div>
             </div>
           </main>
 
-          <aside className={cn('grid min-h-0 min-w-0 gap-3 min-[1180px]:grid-rows-[118px_minmax(0,1fr)_190px]', styles.sideRail)}>
+          <aside className={cn('grid h-full min-h-0 min-w-0 gap-3 min-[1180px]:grid-rows-[118px_minmax(0,1fr)_190px]', styles.sideRail)}>
             <Panel title="Up next" className="h-[118px]">
               <div className="min-h-0 px-3 py-1">
                 {upNext.length > 0 ? (
